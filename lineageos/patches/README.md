# LineageOS Desktop Patch Set

This directory contains source patches applied by
`../scripts/apply_source_patches.sh` and by the ROM build flow. The canonical
application order is `series`; keep this README in the same order so rebases can
quickly answer what each patch is supposed to preserve.

Most patches carry a short `Patch summary:` header as well. This file is the
cross-project index.

## Patches In Apply Order

| Patch | Project | Purpose |
| --- | --- | --- |
| [`external-vendor-availability.patch`](external-vendor-availability.patch) | source root | Marks Google Highway, Skia color-management, and XMP Toolkit SDK helper libraries as `vendor_available` so vendor-side desktop graphics/camera modules can link the upstream modules instead of carrying local shims. |
| [`clang22.patch`](clang22.patch) | source root | Consolidates the cross-project Clang 22 compatibility fixes for external libraries, bionic, Trusty, `device/generic/goldfish`, `hardware/ril`, and `frameworks/native`, covering stricter diagnostics plus Trusty's Clang/Rust host setup. |
| [`external-trusty-lk-arm64-host.patch`](external-trusty-lk-arm64-host.patch) | `external/trusty/lk` | Records optional Trusty Clang host target/link flags in `toolchain.config` and lets ARM64 host envsetup override Rust proc-macro linker args while x86 keeps the existing defaults. |
| [`trusty-kernel-arm64-host.patch`](trusty-kernel-arm64-host.patch) | `trusty/kernel` | Lets Trusty's DTB rules use an env-provided DTC and threads optional Clang host target/runtime flags into host tool and test compiles for ARM64 hosts, while x86 keeps the legacy defaults. |
| [`bison.patch`](bison.patch) | source root | Points the libpcap, one-true-awk, and iproute2 Bison genrules at the checked-in build-tools data directory so parser generation works with project-managed prebuilts. Mesa 26.1.5 no longer needs this workaround. |
| [`system-bpf-vmlinux-6.12.patch`](system-bpf-vmlinux-6.12.patch) | `system/bpf` | Carries BTF-derived ARM64 and x86-64 `vmlinux.h` files for the pinned Android 16 6.12.74 GKIs and exports them directly, matching Android 17's checked-in-header model after newer kernel prebuilts stopped shipping `vmlinux`. |
| [`external-stg-arm64-host-page-size.patch`](external-stg-arm64-host-page-size.patch) | `external/stg` | Removes `libjemalloc5` from the ARM64 musl host variant of `stg`/`stgdiff` so ABI dump generation runs on 16 KiB-page ARM64 hosts while x86 host variants keep the existing allocator. |
| [`prebuilts-arm64-host.patch`](prebuilts-arm64-host.patch) | source root | Enables ARM64 host variants for checked-in build-tool, JDK, and Rust prebuilt module definitions; the build scripts provide the matching `linux-arm64` payloads. |
| [`external-musl-lfs64-compat.patch`](external-musl-lfs64-compat.patch) | `external/musl` | Restores public musl header visibility and weak LFS64 alias symbols (`open64`, `stat64`, `mmap64`, etc.) needed by the Rust musl host stdlib shim on ARM64 hosts. |
| [`external-rust-android-crates-io-arm64-host-cross.patch`](external-rust-android-crates-io-arm64-host-cross.patch) | `external/rust/android-crates-io` | Enables required Rust host-cross modules, including bindgen-related crates, for ARM64 host builds. |
| [`external-rust-rutabaga-gfx-nativewindow-ahb.patch`](external-rust-rutabaga-gfx-nativewindow-ahb.patch) | `external/rust/rutabaga_gfx` | Adapts AHardwareBuffer export to this branch's `nativewindow` Rust API so Android gfxstream AHB exports compile. |
| [`external-crosvm.patch`](external-crosvm.patch) | `external/crosvm` | Adapts the pinned crosvm Android modules to this branch's Wayland protocol code-generation module and exported include layout. |
| [`external-epson-inkjet-printer-escpr-gfxstream-license.patch`](external-epson-inkjet-printer-escpr-gfxstream-license.patch) | `external/epson-inkjet-printer-escpr` | Makes the branch-local X11 license kind visible to gfxstream's bundled xcb headers. |
| [`external-cronet-arm64-host-cross.patch`](external-cronet-arm64-host-cross.patch) | `external/cronet` | Makes Cronet host tools and Rust pieces host-cross capable while keeping x86-specific compiler flags scoped to x86 targets. |
| [`external-sdv-vsomeip-arm64-host-cross.patch`](external-sdv-vsomeip-arm64-host-cross.patch) | `external/sdv/vsomeip` | Restricts SSE flags to x86_64 musl variants so the module graph can be generated on ARM64 hosts without x86-only flags leaking into ARM64 builds. |
| [`external-vulkan-headers-android-build.patch`](external-vulkan-headers-android-build.patch) | `external/vulkan-headers` | Restores Android Soong/Bazel build modules after the local manifest pins Khronos Vulkan-Headers to API revision 1.4.341. |
| [`prebuilts-abi-dumps-vulkan-1.4.341.patch`](prebuilts-abi-dumps-vulkan-1.4.341.patch) | `prebuilts/abi-dumps/ndk` | Refreshes every finalized x86_64 and arm64 `libvulkan` NDK ABI reference dump (API 24-36) that changed when the Vulkan headers were pinned to API revision 1.4.341. |
| [`prebuilts-abi-dumps-vulkan-llndk-1.4.341.patch`](prebuilts-abi-dumps-vulkan-llndk-1.4.341.patch) | `prebuilts/abi-dumps/vndk` | Refreshes the Android 16 `202504` arm64 and x86_64 `libvulkan` LLNDK references from the two Ika product builds, keeping the late-stage ABI check aligned with Vulkan-Headers 1.4.341. |
| [`external-minigbm.patch`](external-minigbm.patch) | `external/minigbm` | Allows mediaswcodec to read the Cuttlefish graphics debug property type used by minigbm debug probes, eliminating the matching enforcing-mode AVCs. |
| [`external-deqp-deps-glslang-validator-host.patch`](external-deqp-deps-glslang-validator-host.patch) | `external/deqp-deps/glslang` | Adds the host `glslangValidator` module expected by Mesa 26.1.5's generated SPIR-V header genrules, built from the existing dEQP glslang source with its generated build-info and intrinsic headers. |
| [`external-mesa3d.patch`](external-mesa3d.patch) | `external/mesa3d` | Carries generated Android/gfxstream Soong integration onto Mesa 26.1.5; preserves the AHardwareBuffer, DRM modifier, BGRA, and Vulkan-profile behavior needed by Android; and includes the validated push-template, driver/ID-property, promoted feature/property consistency, video-capability, global-priority, device-unwind, map-memory2, maintenance6 bind-status, guest device-memory-report, and supported-extension struct-ownership fixes. Written against `6a02618ccf6c`. |
| [`hardware-google-gfxstream-opengles-3.2.patch`](hardware-google-gfxstream-opengles-3.2.patch) | `hardware/google/gfxstream` | Completes the gfxstream GLES 3.2 path: capability probing and `ANDROID_EMU_gles_max_version_3_2` advertisement, cube-map-array guest state, sRGB-decode texture parameters, and safe sampler-unit validation, with fallback through ES 3.1/3.0/2. |
| [`hardware-google-gfxstream-guest-egl-attrib-safety.patch`](hardware-google-gfxstream-guest-egl-attrib-safety.patch) | `hardware/google/gfxstream` | Fixes an uninitialized-output-parameter hazard in the guest `eglDisplay::getAttribValue()`: an unrecognized attribute index returned `EGL_FALSE` without writing `*value`, so callers that don't check the boolean return (a real pattern this file already special-cases for several other attributes) could read stack garbage instead of a defined value. Found while diagnosing CarX Highway Racing (Unity 2022.3) rendering solid black after its ES 3.2 context request was correctly rejected by the host/guest version negotiation. |
| [`hardware-google-gfxstream-compressed-texture-passthrough-guard.patch`](hardware-google-gfxstream-compressed-texture-passthrough-guard.patch) | `hardware/google/gfxstream` | Guards compressed-texture passthrough behind gfxstream's native texture decompression feature flag so games do not receive advertised host compressed-format paths that the translator cannot safely pass through; this favors correct fallback/decompression over pink or missing textures until each passthrough format is verified. |
| [`hardware-google-gfxstream-sparse-astc-native.patch`](hardware-google-gfxstream-sparse-astc-native.patch) | `hardware/google/gfxstream` | Keeps sparse compressed Vulkan images on the host-native path because gfxstream's multi-image decompression representation cannot reproduce guest sparse bindings for its private mip images; non-sparse ASTC/ETC2 emulation remains unchanged. |
| [`hardware-google-gfxstream-sparse-compressed-feature-contract.patch`](hardware-google-gfxstream-sparse-compressed-feature-contract.patch) | `hardware/google/gfxstream` | Keeps Vulkan sparse-binding advertisement coherent with compressed-texture emulation: hosts missing a natively advertised compressed set no longer expose sparse binding, while hosts such as Honeykrisp that natively support the set retain sparse images. |
| [`hardware-google-gfxstream-gles1-alpha-test-core-profile.patch`](hardware-google-gfxstream-gles1-alpha-test-core-profile.patch) | `hardware/google/gfxstream` | Prevents legacy GLES1 `glAlphaFunc`/`glAlphaFuncx` commands from tail-calling an unavailable host fixed-function entry point when gfxstream uses OpenGL core profile or a GLES backing context, avoiding a crosvm-wide null-function-pointer crash. |
| [`hardware-google-camera-emulated-apex-key.patch`](hardware-google-camera-emulated-apex-key.patch) | `hardware/google/camera` | Signs the EmulatedCamera provider APEX with the shared vendor HAL key (`com.android.hardware.key`) so it matches the v4l2 variant; with mismatched keys apexd refuses multi-install selection and activates neither, leaving the guest with zero cameras (camera apps and Chromium's Cardboard VR QR scanner then crash). |
| [`hardware-interfaces-audio-input-pacing.patch`](hardware-interfaces-audio-input-pacing.patch) | `hardware/interfaces` | Makes the Cuttlefish AIDL capture path use ALSA as its sole pacing source: disables MonoPipe's duplicate input throttle, waits for complete captured buffers instead of immediately substituting silence, and limits the emulator skip/sleep workaround to playback. |
| [`art.patch`](art.patch) | `art` | Makes native-bridge namespaces drop APK-embedded `apk.zip!/lib/...` search paths. ARM64 apps running through x86_64 native bridge are more reliable when translated libraries are extracted as real files. |
| [`device-generic-goldfish.patch`](device-generic-goldfish.patch) | `device/generic/goldfish` | Splits the Ranchu radio service build inputs into reusable defaults and filegroups so the pinned Cuttlefish radio APEX can reuse the implementation with its own rc and metadata. |
| [`device-generic-goldfish-sensors-transport-compat.patch`](device-generic-goldfish-sensors-transport-compat.patch) | `device/generic/goldfish` | Adds split CONTROL/DATA routing to the Goldfish sensors transport API so Cuttlefish's separate sensors vconsoles build and route traffic correctly while Ranchu/qemud keeps its single-fd behavior. |
| [`device-google-cuttlefish.patch`](device-google-cuttlefish.patch) | `device/google/cuttlefish` | Adds the Cuttlefish device foundation for desktop products: desktop product entries, ARMv8.2-A minimum ARM64 config, PC feature declarations, batteryless health behavior, graphics property plumbing, ARM64 host accelerated GPU enablement, a permanently available command-line dEQP harness, desktop boot defaults, ADB/provisioning setup, x86_64/native-bridge product config, ROM-first AVB key lookup, OTA vendor post-install metadata derived from the board filesystem type, and boot-ramdisk snd-aloop exclusion (both architectures) so virtio-snd stays ALSA card 0 and guest audio reaches the host. It also removes the AIDL HAL's advertised MMAP routes and retains the legacy MMAP policy properties, forcing AAudio onto the functional mixer path instead of Cuttlefish's VTS-only MMAP simulator. The desktop image advertises gfxstream support without opting into program-binary link-status handling, avoiding corrupt cached-shader rendering while retaining source shader compilation. |
| [`device-google-cuttlefish-light-compat.patch`](device-google-cuttlefish-light-compat.patch) | `device/google/cuttlefish` | Disables the newer Cuttlefish light APEX because this branch freezes the light AIDL interface at V2 while the pinned service requires V3. |
| [`device-google-cuttlefish-health-compat.patch`](device-google-cuttlefish-health-compat.patch) | `device/google/cuttlefish` | Builds the pinned Cuttlefish health HAL against this branch's V4 health AIDL and removes fields introduced by newer revisions. |
| [`device-google-cuttlefish-sepolicy-compat.patch`](device-google-cuttlefish-sepolicy-compat.patch) | `device/google/cuttlefish` | Adapts pinned Cuttlefish system-ext policy to this branch's platform domains, Trusty service labels, and sepolicy API. |
| [`device-google-cuttlefish-proto-editions.patch`](device-google-cuttlefish-proto-editions.patch) | `device/google/cuttlefish` | Enables protobuf editions for the Cuttlefish guest-configuration proto that declares edition 2023. |
| [`device-google-cuttlefish-secure-env-cppcose-compat.patch`](device-google-cuttlefish-secure-env-cppcose-compat.patch) | `device/google/cuttlefish` | Adapts secure-env remote provisioning to this branch's generic cppcose signing helper. |
| [`frameworks-av.patch`](frameworks-av.patch) | `frameworks/av` | Raises software AVC encoder advertised and actual limits to support high-resolution desktop capture/playback, including larger sizes, higher bitrate, and AVC level 5.1. |
| [`frameworks-native-logspam.patch`](frameworks-native-logspam.patch) | `frameworks/native` | Demotes the benign InputDispatcher task-input-sink token correction that is expected in desktop/freeform mode, keeping the self-heal without flooding error logs. |
| [`frameworks-native-vulkan-profile-timestamps.patch`](frameworks-native-vulkan-profile-timestamps.patch) | `frameworks/native` | Lets explicitly opted-in Ika products expose Vulkan display timing without changing the default SurfaceFlinger reliability policy. |
| [`frameworks-base.patch`](frameworks-base.patch) | `frameworks/base` | Applies the main platform and SystemUI desktop contract: desktop/freeform defaults, show-desktop shell plumbing, QS cleanup/blocklisting plus the compact media/notification/alarm audio mixer, keyguard clock suppression for desktop images, storage reporting tweaks, display/power behavior, double-tap shade toggling, status bar/shade adjustments, microG permission support, missing recents directory log cleanup, fullscreen compatibility for apps categorized as games, ScanPackageUtils 16 KB alignment checks for system apps, and a one-shot informational 16 KB page-size compatibility notification. |
| [`frameworks-base-native-bridge.patch`](frameworks-base-native-bridge.patch) | `frameworks/base` | Adjusts package/native-library handling for x86_64-to-ARM64 native bridge installs so bridged APK native libraries are extracted as real files after ABI selection, while preserving normal native app behavior. |
| [`frameworks-base-settingsprovider-ota.patch`](frameworks-base-settingsprovider-ota.patch) | `frameworks/base` | Adds an overlayable SettingsProvider default for the automatic OTA-update setting and loads it into the global settings database. |
| [`frameworks-base-settingsprovider-pointer-speed.patch`](frameworks-base-settingsprovider-pointer-speed.patch) | `frameworks/base` | Seeds the desktop default mouse pointer speed at the slowest position (`def_pointer_speed=-7`) so the Settings "Pointer speed" slider starts fully to the left on first boot. |
| [`system-core.patch`](system-core.patch) | `system/core` | Copies the host-supplied `ro.boot.timezone` into `persist.sys.timezone` during init so the guest boots with the host timezone. |
| [`system-sepolicy.patch`](system-sepolicy.patch) | `system/sepolicy` | Adds minimal desktop SELinux policy for expected VM behavior, including quieting known benign denials, allowing shell display-density property checks used by host tests, and labeling ika's SurfaceFlinger present-timestamp vendor property. |
| [`system-sepolicy-checkfc-arm64-getopt.patch`](system-sepolicy-checkfc-arm64-getopt.patch) | `system/sepolicy` | Stores `checkfc`'s `getopt()` result in an `int` so ARM64 hosts correctly see the `-1` end-of-options sentinel instead of falling through to usage output. |
| [`packages-apps-Launcher3.patch`](packages-apps-Launcher3.patch) | `packages/apps/Launcher3` | Converts Launcher3/Quickstep into the primary desktop shell: taskbar-first navigation, desktop app drawer behavior, taskbar context menus, pinning, bubbles integration, freeform launch coordination, responsive device profiles, dynamic display refresh, show-desktop plumbing, taskbar popup/live-bounds fixes, visual effects, deep shortcuts, log cleanup, and broad test coverage. |
| [`packages-apps-Settings.patch`](packages-apps-Settings.patch) | `packages/apps/Settings` | Disables the page-size warning notification for this desktop VM and replaces phone-oriented Settings shortcuts with Storage and Apps shortcuts. |
| [`vendor-lineage.patch`](vendor-lineage.patch) | `vendor/lineage` | Adds Lineage-side desktop product plumbing: optional app exclusions, empty launcher workspace overlays, and desktop defaults that belong in the Lineage vendor layer rather than AOSP framework/device projects. |
| [`vendor-lineage-arm64-host-tools.patch`](vendor-lineage-arm64-host-tools.patch) | `vendor/lineage` | Uses native ARM64 host `lz4`/`pahole` for kernel builds and falls back to native ImageMagick `mogrify` when the checked-in x86 prebuilts cannot execute, while x86 hosts keep the prebuilt paths. |
| [`build-soong-arm64-host.patch`](build-soong-arm64-host.patch) | `build/soong` | Adds ARM64 host support across Soong's host tags, install paths, Clang/bindgen selection, Rust host-cross modules, proc macros, and JDK path handling, while keeping x86-hosted `linux_musl_arm64` Rust artifacts on the musl ABI with musl CRT/libc deps. |
| [`build-soong-proto-editions.patch`](build-soong-proto-editions.patch) | `build/soong` | Adds an opt-in Soong proto property that passes `--experimental_editions` to protoc for editioned proto files. |
| [`build-make-arm64-host.patch`](build-make-arm64-host.patch) | `build/make` | Uses the active host prebuilt tag for LLVM tools, compiler-rt/libc++ paths, and host build rules so ARM64 hosts use ARM64 prebuilts. |
| [`build-make-zipalign-device.patch`](build-make-zipalign-device.patch) | `build/make` | Builds `zipalign` for device images as well as host tools so both desktop ROMs can run APK alignment diagnostics on-device. |
| [`trusty-user-base-arm64-host-boringssl.patch`](trusty-user-base-arm64-host-boringssl.patch) | `trusty/user/base` | Allows Trusty's host BoringSSL archive to build on ARM64 Linux hosts while preserving the existing x86-64 host path. |
| [`frameworks-libs-binary-translation-arm64-host.patch`](frameworks-libs-binary-translation-arm64-host.patch) | `frameworks/libs/binary_translation` | Broadens binary-translation host helper defaults so required host-side tools can be built from ARM64 hosts without x86-only defaults. |
| [`build-make-releasetools.patch`](build-make-releasetools.patch) | `build/make` | Fixes target-files signing for embedded boot OTA zips that are intentionally not regenerated, and preserves signed `boot_16k.img` when present. |
| [`build-release.patch`](build-release.patch) | `build/release` | Sets release aconfig defaults for desktop builds: keeps SystemUI Quick Settings on the classic implementation and enables Launcher3 desktop visual effects and mouse behavior, Bubble Anything with its trunk stability fixes, PiP box shadows, and cutout-aware bubble-bar behavior in generated release flag sets. |

## Maintenance Notes

- Add new patches to `series`; the build scripts use that file as the source of
  truth for project and order.
- Use `.` in `series` only for patches whose paths are rooted at the Android
  source checkout rather than an individual repo project.
- Keep broad platform behavior in `frameworks-base.patch` and native-bridge
  package manager behavior in `frameworks-base-native-bridge.patch`.
- If a patch exists only to avoid a rebase conflict, say that in both the patch
  header and this README.
